<html>

<body>
    <datalist id="statusList">
        <option>Ready to start</option>
        <option>Watching</option>
        <option>Dropped</option>
        <option>Finished</option>
    </datalist>

<h2>Employees</h2>
{{#anime}}
    <p>
        Название: {{nameEn}} / {{nameRu}} <br>
        Эпизоды: {{episodes}} <br>
        Дата выхода: {{airedOn}} <br>
    </p>
    <p>
        <label>
            Статус: <input name="status" value="{{status}}">
            Оценка: <input name="score" value="{{score}}">

        </label>
    </p>
{{/anime}}
<form method="post" >
    <label> Кек
        <input name="nameEn" {{#anime}} value="{{anime.nameEn}}" {{/anime}} >
        <input name="nameRu" {{#anime}} value="{{anime.nameRu}}" {{/anime}} >
        <input name="status" list="statusList" {{#anime}} value="{{anime.status}}" {{/anime}} >
        <input name="score" {{#anime}} value="{{anime.score}}" {{/anime}} >
        <input name="episodes" {{#anime}} value="{{anime.episodes}}" {{/anime}} >
        <input name="airedOn" type="date" {{#anime}} value="{{anime.airedOn}}" {{/anime}} >
        <input name="shikiId" {{#anime}} value="{{anime.shikiId}}" {{/anime}} hidden>

    </label>
    <br>
    <label>
        Дата начала просмотра: <input name="startDate" type="date" {{#startDate}} value="{{startDate}}" {{/startDate}}>
        Дата окончания просмотра: <input name="endDate" type="date" {{#endDate}} value="{{endDate}}" {{/endDate}}>
    </label>
    <input value="Сохранить" type="submit">
</form>
</body>
</html>
<html>

<body>
    <datalist id="statusList">
        <option>Ready to start</option>
        <option>Watching</option>
        <option>Dropped</option>
        <option>Finished</option>
    </datalist>
    <datalist id="ongoingList">
        <option>yes</option>
        <option>no</option>
    </datalist>

    <h2>Добавление/Редактирование аниме</h2>
    <div>
        <a href="../../title"><--</a>
    </div>

    <form method="post">
        <label>
            Название: <input name="title" {{#anime}} value="{{anime.title}}" {{/anime}}><br>
            Оригинальное название: <input name="originalTitle" {{#anime}} value="{{anime.originalTitle}}" {{/anime}}><br>
            Статус: <input name="status" list="statusList" {{#anime.status}} value="{{anime.status}}" {{/anime.status}} ><br>
            Оценка: <input name="score" type="number" min="1" max="5"  {{#anime.score}} value="{{anime.score}}" {{/anime.score}} ><br>
            Эпизоды: <input name="episodes" {{#anime}} value="{{anime.episodes}}" {{/anime}}><br>
            Дата выхода: <input name="releaseDate" type="date" {{#anime}} value="{{anime.releaseDate}}" {{/anime}}>
            <input name="shikiId" {{#anime}} value="{{anime.shikiId}}" {{/anime}} hidden>
            <input name="releaseYear" {{#anime}} value="{{anime.releaseYear}}" {{/anime}} hidden>
        </label>
        <br>
        <label>
            Просмотрено серий <input name="episodesEnd" type="number" min="0" {{#anime.episodesEnd}} value="{{anime.episodesEnd}}" {{/anime.episodesEnd}}>
            Просмотр начат в онгоинге? <input name="ongoingStart" list="ongoingList" {{#anime.ongoingStart}} value="{{anime.ongoingStart}}" {{/anime.ongoingStart}}><br>
            Дата начала просмотра: <input name="startDate" type="date" {{#anime.startDate}} value="{{anime.startDate}}" {{/anime.startDate}}>
            Дата окончания просмотра: <input name="endDate" type="date" {{#anime.endDate}} value="{{anime.endDate}}" {{/anime.endDate}}>
        </label><br>
        <table>
            <tr>
                Студии:
            </tr>
            {{#studioList}}
                <tr>
                    {{name}}
                </tr>
            {{/studioList}}

        </table>
        <input value="Сохранить" type="submit">
        <br>
        {{#add}}
            <p>
                <img alt="haruhi" {{#image}}src="{{image}}"{{/image}} width="200" height="300">
            </p>
        {{/add}}
    </form>
    {{#edit}}
        <form method="post" action="./delete">
            <input name="id" {{#anime}} value="{{anime.id}}" {{/anime}} hidden>
            <input value="Удалить" type="submit">
        </form>
    {{/edit}}
</body>
</html>
<html>

<body>
    <datalist id="statusList">
        <option>Ready to start</option>
        <option>In progress</option>
        <option>Dropped</option>
        <option>Finished</option>
    </datalist>

    <h2>Добавление/Настройка фильма</h2>
    <div>
        <a href="../../title"><--</a>
    </div>
    <form method="post" >
        <label>
            Название: <input name="title" {{#movie}} value="{{movie.title}}" {{/movie}}><br>
            Оригинальное название: <input name="originalTitle" {{#movie}} value="{{movie.originalTitle}}" {{/movie}}><br>
            Статус: <input name="status" list="statusList" {{#movie.status}} value="{{movie.status}}" {{/movie.status}} ><br>
            Оценка: <input name="score" type="number" min="0" max="5"  {{#movie.score}} value="{{movie.score}}" {{/movie.score}} ><br>
            Дата выхода: <input name="releaseYear" type="number" {{#movie}} value="{{movie.releaseYear}}" {{/movie}}><br>
            Длительность: <input name="duration" {{#movie.duration}} value="{{movie.duration}}" {{/movie.duration}}><br>
            <input name="integId" type="number" value="{{#integId}}{{integId}}{{/integId}}">
            <input name="usrId" {{#movie.usrId}} value="{{movie.usrId}}" {{/movie.usrId}} hidden>
        </label>
        <br>
        <label>
            Дата просмотра: <input name="endDate" type="date" {{#movie.endDate}} value="{{movie.endDate}}" {{/movie.endDate}}><br>
        </label><br>
        <input value="Сохранить" type="submit">
        <br>
        {{#add}}
            <p>
                <img alt="haruhi" {{#image}}src="{{image}}"{{/image}} width="200" height="300">
            </p>
        {{/add}}
    </form>
    {{#edit}}
        <form method="post" action="./delete">
            <input name="id" {{#movie}} value="{{movie.id}}" {{/movie}} hidden>
            <input value="Удалить" type="submit">
        </form>
    {{/edit}}
</body>
</html>